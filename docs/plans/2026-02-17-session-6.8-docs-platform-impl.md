# Session 6.8: Docs Platform Detection + Playbooks — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Make AID Orchestrator docs-platform-agnostic by adding detection, platform playbooks, and parametrizing hardcoded Docusaurus references.

**Architecture:** aid-setup detects docs platform → writes to project-profile.yaml → agents/skills load platform-specific playbook (`docs-{platform}.md`). Existing Docusaurus instructions consolidated into `docs-docusaurus.md`. Main behavioral files parametrized to reference `{project.docs.*}`.

**Tech Stack:** Markdown prompt engineering (no executable code). All changes are .md and .yaml files in `plugins/aid-orchestrator/`.

---

### Task 1: Create docs-docusaurus.md playbook

**Files:**
- Create: `plugins/aid-orchestrator/defaults/playbooks/docs-docusaurus.md`

**Step 1: Write the Docusaurus docs playbook**

This consolidates all Docusaurus-specific instructions currently scattered across 10+ files.

```markdown
# Docusaurus Documentation Playbook

> Loaded when `project.docs.platform == docusaurus`. Contains platform-specific rules
> for Docusaurus projects. Generic docs responsibilities are in `playbooks/docs.md`.

## File Format

- Preferred: `.mdx` (supports JSX components in docs)
- Also supported: `.md` (standard Markdown)
- Documentation root: `{project.docs.path}` (default: `docs/`)

## Frontmatter Requirements

Every documentation file in `{project.docs.path}` MUST have YAML frontmatter:

```yaml
---
title: "Page Title"
sidebar_label: "Short Label"
last_updated: "YYYY-MM-DD"
status: draft|review|published
audience: developer|pm|user
complexity: beginner|intermediate|advanced
---
```

**Required fields:** `title`, `sidebar_label`, `last_updated`
**Optional but recommended:** `status`, `audience`, `complexity`

## MDX Escaping Rules

MDX is stricter than Markdown — these cause build failures if not followed:

- `{braces}` in prose text MUST be in backticks: `` `{braces}` ``
- `<` in comparisons MUST use `&lt;` or the word "under" / "less than"
- JSX-like tags (`<Component>`) MUST be valid JSX or escaped
- Mermaid code blocks MUST use ` ```mermaid ` fence (not inline)

## Sidebar Configuration

If `sidebars.js` (or `sidebars.ts`) is modified:
- Document IDs must match file paths (no numeric prefix, no `.md` extension)
- Category structure should be logical and match directory layout
- No orphaned pages (every doc in `{project.docs.path}` should appear in sidebar)

## Build Verification

After writing or modifying docs, verify the build:

```bash
cd {project.docs.path}
npm run build
```

Exit code 0 = success. Any error (broken links, MDX parse failure, missing sidebar entry) must be fixed before committing.

## Quality Checklist

- [ ] Frontmatter present with required fields (`title`, `sidebar_label`, `last_updated`)
- [ ] MDX escaping correct (braces in backticks, `<` escaped, JSX tags valid)
- [ ] `last_updated` set to today's date on modified files
- [ ] No broken internal links (`[text](./relative-path.md)`)
- [ ] Build passes: `npm run build` in `{project.docs.path}` exits with code 0
- [ ] Sidebar entries present for new pages
```

**Step 2: Commit**

```bash
git add plugins/aid-orchestrator/defaults/playbooks/docs-docusaurus.md
git commit -m "feat(playbooks): create docs-docusaurus.md platform playbook"
```

---

### Task 2: Create docs-generic.md playbook

**Files:**
- Create: `plugins/aid-orchestrator/defaults/playbooks/docs-generic.md`

**Step 1: Write the generic Markdown docs playbook**

```markdown
# Generic Markdown Documentation Playbook

> Loaded when `project.docs.platform == generic-markdown` or no specific platform detected.
> Contains minimal rules for plain Markdown documentation projects.

## File Format

- Format: `.md` (standard Markdown)
- Documentation root: `{project.docs.path}` (default: `docs/`)

## Frontmatter

No specific frontmatter is required for generic Markdown projects. If the project has its own conventions, follow them.

Optional recommended frontmatter:
```yaml
---
title: "Page Title"
last_updated: "YYYY-MM-DD"
---
```

## Formatting Rules

- Use standard CommonMark Markdown
- No framework-specific syntax required
- Code blocks with language identifier: ` ```python `
- Internal links as relative paths: `[text](./other-doc.md)`

## Build Verification

No build step required for generic Markdown projects. If the project has a static site generator configured separately, check `project.docs.build_command` in `project-profile.yaml`.

## Quality Checklist

- [ ] Markdown renders correctly (no broken syntax)
- [ ] No broken internal links
- [ ] Code examples are accurate and up to date
- [ ] `last_updated` date set on modified files (if frontmatter is used)
```

**Step 2: Commit**

```bash
git add plugins/aid-orchestrator/defaults/playbooks/docs-generic.md
git commit -m "feat(playbooks): create docs-generic.md platform playbook"
```

---

### Task 3: Add docs platform detection to aid-setup.md

**Files:**
- Modify: `plugins/aid-orchestrator/commands/aid-setup.md:57-66` (after Linter Detection)
- Modify: `plugins/aid-orchestrator/commands/aid-setup.md:164-190` (project-profile.yaml output)

**Step 1: Add Docs Platform Detection table**

After the Linter Detection table (around line 66), insert:

```markdown

**Docs Platform Detection:**

| Indicator File | Platform | Format | Build Command |
|---------------|----------|--------|---------------|
| `docusaurus.config.js` or `docusaurus.config.ts` | `docusaurus` | `mdx` | `npm run build` |
| `mkdocs.yml` | `mkdocs` | `md` | `mkdocs build` |
| `conf.py` + `index.rst` | `sphinx` | `rst` | `make html` |
| `.vitepress/config.js` or `.vitepress/config.ts` | `vitepress` | `md` | `vitepress build` |
| `book.toml` | `mdbook` | `md` | `mdbook build` |
| `docs/` exists, none of above | `generic-markdown` | `md` | _(none)_ |
| No `docs/` directory | `none` | — | — |

Search for indicator files in project root first, then inside `docs/` directory.
```

**Step 2: Add docs to project-profile.yaml output**

In the project-profile.yaml template (Option 3, around line 182, after `databases:`), insert:

```yaml
  docs:
    platform: "{detected platform or 'none'}"
    path: "{detected docs root directory}"
    format: "{mdx|md|rst}"
    build_command: "{platform-specific build command or null}"
    frontmatter_required: {true if docusaurus/vitepress, false otherwise}
```

**Step 3: Add docs to the Step 3 analysis output**

In the "Present Analysis" output block (around line 95), add after `CI/CD:`:

```
Docs:      {platform} ({format}) — build: {build_command}
```

**Step 4: Commit**

```bash
git add plugins/aid-orchestrator/commands/aid-setup.md
git commit -m "feat(aid-setup): add docs platform detection and project-profile.yaml docs section"
```

---

### Task 4: Add docs platform to project-scanner.md

**Files:**
- Modify: `plugins/aid-orchestrator/agents/project-scanner.md:54-59` (Quick Scan DETECT step)
- Modify: `plugins/aid-orchestrator/agents/project-scanner.md:153-159` (profile schema)

**Step 1: Add docs platform detection to Quick Scan step 2**

After line 59 (`CI/CD — GitHub Actions/GitLab CI/Jenkins/CircleCI`), insert:

```markdown
    Docs platform — check for docusaurus.config.js/ts, mkdocs.yml, conf.py, .vitepress/config.*, book.toml in project root or docs/
```

**Step 2: Add docs to profile schema**

After line 159 (`package_managers: []`), insert:

```yaml
  docs:
    platform: "{docusaurus|mkdocs|sphinx|vitepress|mdbook|generic-markdown|none}"
    path: "{detected docs root}"
    format: "{mdx|md|rst}"
    build_command: "{platform build command or null}"
    frontmatter_required: true|false
```

**Step 3: Commit**

```bash
git add plugins/aid-orchestrator/agents/project-scanner.md
git commit -m "feat(scanner): add docs platform detection to quick scan and profile schema"
```

---

### Task 5: Parametrize session-management.md (3 Docusaurus references)

**Files:**
- Modify: `plugins/aid-orchestrator/skills/session-management.md:20,183-187,438`

**Step 1: Fix line 20 (TL;DR rule)**

Replace:
```
10. **UPDATE Docusaurus docs** at session-end (mandatory impact analysis before final commit)
```

With:
```
10. **UPDATE project docs** at session-end (mandatory impact analysis per `playbooks/docs-{project.docs.platform}.md`)
```

**Step 2: Fix lines 183-187 (Session-End Protocol)**

Replace:
```
2. **UPDATE Docusaurus project documentation (docs/)** — THIS IS MANDATORY:
   - Run documentation impact analysis against ALL changes in this session
   - Update affected docs in `{project.paths.docs}` directory
   - If docs changed: verify `npm run build` in docs/
   - If no docs affected: document why not in session file
```

With:
```
2. **UPDATE project documentation** — THIS IS MANDATORY:
   - Run documentation impact analysis against ALL changes in this session
   - Update affected docs in `{project.docs.path}` directory
   - Load platform playbook: `playbooks/docs-{project.docs.platform}.md`
   - If docs changed: verify build with `{project.docs.build_command}` (skip if null/none)
   - If no docs affected: document why not in session file
   - If `project.docs.platform == none`: skip docs update, note in session file
```

**Step 3: Fix line 438 (anti-pattern table)**

Replace:
```
| Skipping Docusaurus docs at session-end | ALWAYS run impact analysis before final commit |
```

With:
```
| Skipping project docs at session-end | ALWAYS run impact analysis before final commit (per docs platform playbook) |
```

**Step 4: Commit**

```bash
git add plugins/aid-orchestrator/skills/session-management.md
git commit -m "feat(session-mgmt): parametrize Docusaurus references to platform-aware docs"
```

---

### Task 6: Parametrize agent-core.md (3 Docusaurus references)

**Files:**
- Modify: `plugins/aid-orchestrator/skills/agent-core.md:279,286,326`

**Step 1: Fix line 279 (workflow overview)**

Replace:
```
End:   Session-End Protocol (Docusaurus docs update + final commit + PM approval for PR/merge + archive)
```

With:
```
End:   Session-End Protocol (docs update per project docs playbook + final commit + PM approval for PR/merge + archive)
```

**Step 2: Fix line 286 (lifecycle protocols)**

Replace:
```
- **Session-End:** Docusaurus docs update + final quality gates + ask PM for PR/merge + archive
```

With:
```
- **Session-End:** docs update (per `playbooks/docs-{project.docs.platform}.md`) + final quality gates + ask PM for PR/merge + archive
```

**Step 3: Fix line 326 (ALWAYS rules)**

Replace:
```
- Update Docusaurus docs at session-end (mandatory impact analysis)
```

With:
```
- Update project docs at session-end (mandatory impact analysis per docs platform playbook)
```

**Step 4: Commit**

```bash
git add plugins/aid-orchestrator/skills/agent-core.md
git commit -m "feat(agent-core): parametrize Docusaurus references to platform-aware docs"
```

---

### Task 7: Parametrize docs.md playbook

**Files:**
- Modify: `plugins/aid-orchestrator/defaults/playbooks/docs.md:24-27,34-35,43-44`

**Step 1: Update Outputs table (lines 24-27)**

Replace:
```
| API docs | Markdown/MDX | `docs/api/` |
| Architecture docs | Markdown/MDX | `docs/architecture/` |
| User docs | Markdown/MDX | `docs/guides/` |
| CHANGELOG entry | Markdown | `CHANGELOG.md` |
```

With:
```
| API docs | `{project.docs.format}` | `{project.docs.path}/api/` |
| Architecture docs | `{project.docs.format}` | `{project.docs.path}/architecture/` |
| User docs | `{project.docs.format}` | `{project.docs.path}/guides/` |
| CHANGELOG entry | Markdown | `CHANGELOG.md` |
```

**Step 2: Update Process steps 4-5 (lines 34-35)**

Replace:
```
4. **Build** — Verify docs build without errors (`npm run build` in docs/)
5. **Review** — Check for broken links, stale examples, MDX escaping
```

With:
```
4. **Build** — Verify docs build: run `{project.docs.build_command}` in `{project.docs.path}` (skip if no build command)
5. **Review** — Check for broken links, stale examples. Load platform-specific rules from `playbooks/docs-{project.docs.platform}.md`
```

**Step 3: Update Quality Criteria (lines 43-44)**

Replace:
```
- [ ] MDX escaping correct (braces, angle brackets)
- [ ] Frontmatter present on all doc pages (title, sidebar_label, last_updated)
```

With:
```
- [ ] Platform-specific formatting rules followed (per `playbooks/docs-{project.docs.platform}.md`)
- [ ] Frontmatter present where required by platform (see platform playbook)
```

**Step 4: Commit**

```bash
git add plugins/aid-orchestrator/defaults/playbooks/docs.md
git commit -m "feat(playbooks): parametrize docs.md for platform-aware documentation"
```

---

### Task 8: Conditionally parametrize docs-reviewer.md

**Files:**
- Modify: `plugins/aid-orchestrator/agents/docs-reviewer.md` (full file)

**Step 1: Update description and identity (lines 3, 7)**

Replace line 3:
```
description: Reviews documentation changes for MDX compliance, frontmatter, and completeness. Use when documentation files are modified to ensure they meet C.I.C.E.R.O. Docusaurus standards.
```

With:
```
description: Reviews documentation changes for compliance, frontmatter, and completeness. Adapts checks based on detected docs platform from project-profile.yaml.
```

Replace line 7:
```
You are a Documentation Reviewer for C.I.C.E.R.O. project. Check all changed .md/.mdx files for compliance with project documentation standards.
```

With:
```
You are a Documentation Reviewer. Check all changed documentation files for compliance with the project's documentation standards. Read `project-profile.yaml` to determine `project.docs.platform` and load the appropriate platform playbook (`playbooks/docs-{project.docs.platform}.md`).
```

**Step 2: Make frontmatter section conditional (lines 11-25)**

Replace:
```
### 1. YAML Frontmatter

Every documentation file in `docs/` must have:
```yaml
---
title: "Page Title"
sidebar_label: "Short Label"
last_updated: "YYYY-MM-DD"
status: draft|review|published
audience: developer|pm|user
complexity: beginner|intermediate|advanced
---
```

Required fields: `title`, `sidebar_label`, `last_updated`
Optional but recommended: `status`, `audience`, `complexity`
```

With:
```
### 1. YAML Frontmatter

**If `project.docs.frontmatter_required == true`** (e.g., Docusaurus, VitePress):

Check that every documentation file in `{project.docs.path}` has frontmatter per the platform playbook. See `playbooks/docs-{project.docs.platform}.md` for required fields.

**If `project.docs.frontmatter_required == false`** (e.g., generic Markdown):

Frontmatter is optional. Skip this check or verify only that existing frontmatter is well-formed YAML.
```

**Step 3: Make MDX section conditional (lines 27-32)**

Replace:
```
### 2. MDX Escaping

- `{braces}` in prose text must be in backticks: `` `{braces}` ``
- `<` in comparisons must use `&lt;` or word "under/less than"
- JSX-like tags must be valid or escaped
- Mermaid code blocks must use ` ```mermaid ` fence
```

With:
```
### 2. Format-Specific Rules

**If `project.docs.format == mdx`** (Docusaurus):
- `{braces}` in prose text must be in backticks: `` `{braces}` ``
- `<` in comparisons must use `&lt;` or word "under/less than"
- JSX-like tags must be valid or escaped
- Mermaid code blocks must use ` ```mermaid ` fence

**If `project.docs.format == md`** (generic, MkDocs, VitePress):
- Standard Markdown syntax rules apply
- No MDX-specific escaping required

**If `project.docs.format == rst`** (Sphinx):
- reStructuredText syntax rules apply
```

**Step 4: Make sidebar section conditional (lines 47-52)**

Replace:
```
### 5. Sidebar Configuration

If `sidebars.js` or equivalent is modified:
- IDs match file paths (no numeric prefix, no `.md` extension)
- Category structure is logical
- No orphaned pages
```

With:
```
### 5. Navigation/Sidebar Configuration

**If `project.docs.platform == docusaurus`:** Check `sidebars.js` — IDs match file paths, no orphaned pages.
**If `project.docs.platform == mkdocs`:** Check `mkdocs.yml` `nav:` section.
**If `project.docs.platform == vitepress`:** Check `.vitepress/config.*` sidebar config.
**Otherwise:** Skip navigation config check.
```

**Step 5: Update scope (line 61-63)**

Replace:
```
Only review files that were changed in the current diff. Run `git diff --name-only` to identify them, then filter for `.md` and `.mdx` files in the `docs/` directory.

Workspace files (`workspace/*.md`) and skill instructions (`.claude/skills/**/*.md`) do NOT need Docusaurus frontmatter — skip those.
```

With:
```
Only review files that were changed in the current diff. Run `git diff --name-only` to identify them, then filter for documentation files in `{project.docs.path}`.

File extensions to check: `.md` always; `.mdx` if `project.docs.format == mdx`; `.rst` if `project.docs.format == rst`.

Workspace files (`workspace/*.md`), skill files, and plugin files do NOT need documentation frontmatter — skip those.
```

**Step 6: Commit**

```bash
git add plugins/aid-orchestrator/agents/docs-reviewer.md
git commit -m "feat(docs-reviewer): make all checks conditional on detected docs platform"
```

---

### Task 9: Cross-reference verification

**Files:**
- Read all modified + created files

**Step 1: Verify consistency**

Check that:
1. `docs-docusaurus.md` contains ALL Docusaurus-specific rules that were removed from other files
2. `docs-generic.md` is a valid fallback for projects without a specific docs platform
3. `aid-setup.md` detection table matches the platforms referenced in `docs-reviewer.md`
4. `project-scanner.md` profile schema matches `aid-setup.md` project-profile.yaml output
5. `session-management.md` no longer contains the word "Docusaurus"
6. `agent-core.md` no longer contains the word "Docusaurus"
7. `docs.md` playbook references `{project.docs.*}` consistently
8. `docs-reviewer.md` checks are properly conditional on `project.docs.platform`
9. All `{project.docs.*}` placeholders are consistent across files

**Step 2: Fix any inconsistencies**

**Step 3: Commit (if fixes needed)**

```bash
git commit -m "fix: cross-reference consistency for docs platform parametrization"
```
