# Session 6.7: Content Quality Loop + Discovered Issues — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add content quality validation to PHASE_CHECK and structured issue reporting for agents.

**Architecture:** Extend PHASE_CHECK with acceptance criteria validation (step 4) and discovered issues triage (step 5). Agents report issues via `## DISCOVERED ISSUES` section in output.md. Decision-policies.yaml gains two new sections: `content_quality` and `discovered_issues`.

**Tech Stack:** Markdown prompt engineering (no executable code). All changes are .md and .yaml files in `plugins/aid-orchestrator/`.

---

### Task 1: Add `content_quality` and `discovered_issues` to decision-policies.yaml

**Files:**
- Modify: `plugins/aid-orchestrator/defaults/policies/decision-policies.yaml:65-96`

**Step 1: Add `content_quality` section**

Insert after line 65 (after the last `auto_decisions` entry, before the `# ─── Escalation Triggers` header):

```yaml

  # Content Quality (acceptance validation after each step)
  - condition: "step output meets all acceptance criteria from plan"
    action: "proceed to next step"
    reason: "content quality verified"

  - condition: "acceptance criteria unclear, step is high-complexity"
    action: "dispatch code-reviewer for detailed review"
    reason: "orchestrator cannot verify domain-specific criteria"

  - condition: "acceptance criteria clearly NOT met after 2 fix cycles"
    action: "escalate to PM"
    reason: "automated fix attempts exhausted"

# ─── Content Quality Rules ─────────────────────────────────────────
content_quality:
  auto_accept_when:
    - all acceptance criteria verifiable from output.md
    - step role is: docs, config, release
    - step has <= 3 acceptance criteria

  review_required_when:
    - step role is: architect, backend, frontend, security
    - step has 5+ acceptance criteria
    - step is target of analysis_group
    - orchestrator cannot determine if criterion is met

  review_agent: code-reviewer
  max_review_fix_cycles: 2
```

**Step 2: Add `discovered_issues` escalation entry**

Insert after line 95 (after the `security finding classified as CRITICAL` trigger, before `# ─── Acceptable Technical Debt`):

```yaml

  - trigger: "agent reports CRITICAL discovered issue"
    action: "HARD STOP — triage discovered issue"
    options: ["auto-fix (dispatch appropriate agent)", "skip (document risk)", "abort EPIC"]

# ─── Discovered Issues Triage ──────────────────────────────────────
discovered_issues:
  critical:
    auto_fix_patterns:
      - pattern: "migration.*fail"
        dispatch: backend
      - pattern: "dependency.*conflict"
        dispatch: backend
      - pattern: "security.*critical"
        dispatch: security
    default_action: escalate_to_pm
    blocks_current_step: true

  high:
    forward_to_later_step: true
    create_backlog_entry: true
    pm_notification: true
    blocks_current_step: false

  medium:
    log_to_improvement_notes: true
    blocks_current_step: false

  info:
    log_to_improvement_notes: true
    blocks_current_step: false
```

**Step 3: Commit**

```bash
git add plugins/aid-orchestrator/defaults/policies/decision-policies.yaml
git commit -m "feat(policies): add content_quality and discovered_issues rules"
```

---

### Task 2: Expand PHASE_CHECK in epic-orchestration.md

**Files:**
- Modify: `plugins/aid-orchestrator/skills/epic-orchestration.md:195-221`

**Step 1: Add Acceptance Validation (item 5) and Discovered Issues Triage (item 6)**

After line 212 (after `c. Merge analysis improvement_notes into step evidence (for Curator)`), insert:

```markdown
5. **Acceptance Validation** (per `decision-policies.yaml` → `content_quality`):
   a. Read agent's `output.md` from `evidence/steps/step_{N}_{role}/output.md`
   b. Read step's acceptance criteria from `plan.json` → `steps[N].outputs` + session file acceptance items
   c. For each acceptance criterion, evaluate:
      - Verifiable from output.md + `git diff`? → verify directly
      - Requires domain knowledge beyond Controller's scope? → mark `needs_review`
   d. Decision (per `content_quality` rules):
      - All criteria clearly met → PASS
      - Any `needs_review` AND step matches `review_required_when` → dispatch `code-reviewer` agent
        - Reviewer APPROVED → PASS
        - Reviewer REJECTED (with feedback) → re-dispatch original agent with reviewer feedback
        - Max `max_review_fix_cycles` (default 2) → then ESCALATION
      - Any criterion clearly NOT met → re-dispatch agent with specific feedback (max 2 cycles → ESCALATION)
   e. Evidence: save review to `evidence/{epic_id}/{run_id}/reviews/step_{N}_{role}_review_{cycle}.md`
6. **Discovered Issues Triage** (per `decision-policies.yaml` → `discovered_issues`):
   a. Parse `## DISCOVERED ISSUES` section from agent's `output.md` (if present)
   b. If no section → skip (no issues reported)
   c. For each issue, extract: severity (`[CRITICAL]`/`[HIGH]`/`[MEDIUM]`/`[INFO]`), description, impact, recommendation
   d. Triage per severity:
      - **CRITICAL:** Check `auto_fix_patterns` → match → dispatch fix agent; no match → ESCALATION. Current step BLOCKED.
      - **HIGH:** Log to `evidence/discovered_issues/`. Forward to later step if natural fit, else create `backlog.md` entry. PM Slack notification (informational). NOT blocking.
      - **MEDIUM/INFO:** Log to `evidence/discovered_issues/`. Add to `improvement_notes` (Curator picks up). NOT blocking.
   e. Evidence: save all issues to `evidence/{epic_id}/{run_id}/discovered_issues/step_{N}.md`
   f. Session file: add CRITICAL/HIGH issues to Session Log
```

**Step 2: Expand Auto-Decision Logic block**

Replace lines 214-221 (the existing Auto-Decision Logic block) with:

```markdown
**Auto-Decision Logic:**
```
outputs_present AND within_scope AND acceptance_met → NEXT_PHASE
outputs_present AND within_scope AND acceptance_unclear → dispatch code-reviewer (if review_required_when)
outputs_present AND acceptance_not_met → re-dispatch agent with feedback (max 2 cycles)
outputs_present AND scope_violation → re-dispatch (max 1 retry)
no_outputs OR error → ESCALATION
parallel_merge_conflict → ESCALATION
analysis_critical_findings → ESCALATION
discovered_issue_CRITICAL → triage (auto-fix or ESCALATION)
discovered_issue_HIGH → log + backlog + PM notification (non-blocking)
review_fix_cycles_exhausted → ESCALATION
```
```

**Step 3: Commit**

```bash
git add plugins/aid-orchestrator/skills/epic-orchestration.md
git commit -m "feat(orchestration): expand PHASE_CHECK with acceptance validation and discovered issues triage"
```

---

### Task 3: Expand PHASE_CHECK in run-epic.md + Update Agent Prompt Template

**Files:**
- Modify: `plugins/aid-orchestrator/commands/run-epic.md:246-279` (PHASE_CHECK)
- Modify: `plugins/aid-orchestrator/commands/run-epic.md:183-187` (agent prompt template)

**Step 1: Expand PHASE_CHECK decision table**

After line 259 (the `No outputs OR agent error → ESCALATION` table row), add:

```markdown
| Acceptance criteria met (verified from output.md) | → NEXT_PHASE |
| Acceptance unclear, review_required_when matches | Dispatch code-reviewer, then re-check |
| Acceptance clearly NOT met | Re-dispatch agent with feedback (max 2 cycles), then ESCALATION |
| CRITICAL discovered issue reported | Triage: auto-fix or ESCALATION (step blocked) |
| HIGH discovered issue reported | Log to backlog + PM Slack notification (non-blocking) |
```

**Step 2: Add acceptance + issues check as item 4**

After line 261 (after `3. For parallel groups...`), insert:

```markdown
4. **Acceptance Validation** (per `decision-policies.yaml` → `content_quality`):
   - Read agent's `output.md` from evidence
   - Compare against step acceptance criteria from plan.json
   - Auto-accept: simple roles (docs, config, release), ≤3 criteria, all verifiable
   - Review required: complex roles (architect, backend, frontend, security), 5+ criteria, unclear items
   - If review needed: dispatch `code-reviewer` agent with output.md + plan criteria + git diff
   - Review result: APPROVED → proceed; REJECTED → re-dispatch original agent with feedback
   - Max 2 review-fix cycles, then ESCALATION

5. **Discovered Issues Triage** (per `decision-policies.yaml` → `discovered_issues`):
   - Parse `## DISCOVERED ISSUES` from agent's output.md
   - CRITICAL → check auto_fix_patterns → dispatch fix agent or ESCALATION (step blocked)
   - HIGH → log to evidence + backlog.md + PM Slack (non-blocking)
   - MEDIUM/INFO → log to evidence + improvement_notes (non-blocking)
   - Evidence: `evidence/{epic_id}/{run_id}/discovered_issues/step_{N}.md`
```

**Step 3: Update agent prompt template deliverables**

After line 186 (`2. Output summary (what you did, what you created, decisions made)`), insert:

```markdown
   3. (Optional) If you discover problems outside your task scope, add a `## DISCOVERED ISSUES` section:
      - Format: `- **[SEVERITY]** Description` where SEVERITY is CRITICAL/HIGH/MEDIUM/INFO
      - Each issue needs: `- Impact:` and `- Recommendation:` sub-bullets
      - CRITICAL = blocks further work. HIGH = should be addressed. MEDIUM/INFO = nice to know.
      - Only report genuine issues — do not pad this section.
```

**Step 4: Commit**

```bash
git add plugins/aid-orchestrator/commands/run-epic.md
git commit -m "feat(run-epic): add acceptance validation, discovered issues triage, and issue reporting to agent prompt"
```

---

### Task 4: Document DISCOVERED ISSUES format in agent-core.md

**Files:**
- Modify: `plugins/aid-orchestrator/skills/agent-core.md:200-204`

**Step 1: Add discovered issues to sub-agent responsibilities**

After line 204 (`- Do NOT modify session file or workspace files`), insert:

```markdown
   - Report discovered issues using `## DISCOVERED ISSUES` section in output (if any found):
     ```
     ## DISCOVERED ISSUES

     - **[CRITICAL]** {description}
       - Impact: {what breaks or is blocked}
       - Recommendation: {fix now / defer / escalate}

     - **[HIGH]** {description}
       - Impact: {consequence if not addressed}
       - Recommendation: {fix now / add to backlog / notify PM}

     - **[MEDIUM]** {description}
       - Impact: {technical debt or minor risk}
       - Recommendation: {improvement suggestion}

     - **[INFO]** {description}
       - Impact: {informational only}
       - Recommendation: {for awareness}
     ```
     Severities: CRITICAL (blocks step), HIGH (backlog + PM), MEDIUM/INFO (improvement_notes).
     Only report genuine issues found during work — do not pad.
```

**Step 2: Commit**

```bash
git add plugins/aid-orchestrator/skills/agent-core.md
git commit -m "feat(agent-core): document DISCOVERED ISSUES output format"
```

---

### Task 5: Add DISCOVERED ISSUES instruction to all 9 role playbooks

**Files:**
- Modify: `plugins/aid-orchestrator/defaults/playbooks/architect.md`
- Modify: `plugins/aid-orchestrator/defaults/playbooks/backend.md`
- Modify: `plugins/aid-orchestrator/defaults/playbooks/domain.md`
- Modify: `plugins/aid-orchestrator/defaults/playbooks/frontend.md`
- Modify: `plugins/aid-orchestrator/defaults/playbooks/qa.md`
- Modify: `plugins/aid-orchestrator/defaults/playbooks/security.md`
- Modify: `plugins/aid-orchestrator/defaults/playbooks/observability.md`
- Modify: `plugins/aid-orchestrator/defaults/playbooks/docs.md`
- Modify: `plugins/aid-orchestrator/defaults/playbooks/release.md`

**Step 1: In each playbook, insert DISCOVERED ISSUES block**

In each playbook, find the closing ` ``` ` of the `improvement_notes:` YAML block (this is the line right before `**Record when you see:**`). Insert after that closing fence:

```markdown

## Discovered Issues

If you encounter problems **outside your task scope** during work, report them in your output using `## DISCOVERED ISSUES`:

```
## DISCOVERED ISSUES

- **[SEVERITY]** Description of the problem
  - Impact: What is affected
  - Recommendation: Fix now / defer / escalate
```

Severities:
- **CRITICAL** — blocks your work or other steps. Controller will auto-fix or escalate to PM.
- **HIGH** — should be addressed but doesn't block you. Goes to backlog + PM notification.
- **MEDIUM** — technical debt or minor improvement. Curator picks up later.
- **INFO** — for awareness only.

Only report genuine issues. Do not create this section if you found no issues.
```

**Important:** This block goes between the `improvement_notes` YAML example and the `**Record when you see:**` line. The exact insertion line differs per playbook:
- architect.md: after line ~69 (before line ~71 `**Record when you see:**`)
- backend.md: after line ~73
- domain.md: after line ~67
- frontend.md: after line ~70
- qa.md: after line ~71
- security.md: after line ~72
- observability.md: after line ~67
- docs.md: after line ~70
- release.md: after line ~69

**Step 2: Commit**

```bash
git add plugins/aid-orchestrator/defaults/playbooks/
git commit -m "feat(playbooks): add DISCOVERED ISSUES section to all 9 role playbooks"
```

---

### Task 6: Update Integration with Session Management in epic-orchestration.md

**Files:**
- Modify: `plugins/aid-orchestrator/skills/epic-orchestration.md:560-573`

**Step 1: Add review and discovered issues evidence to Integration section**

In the "Integration with Session Management" section, after item 2 (`On each PHASE_CHECK`), expand it:

Find the current text (around lines 560-563):
```
2. **On each PHASE_CHECK:** Update session file:
   - Mark completed phase acceptance items as checked
   - Add step status + commit hash to Session Log
```

Replace with:
```
2. **On each PHASE_CHECK:** Update session file:
   - Mark completed phase acceptance items as checked/failed based on acceptance validation
   - If review dispatched: log review result (approved/rejected + feedback summary)
   - If discovered issues: log CRITICAL/HIGH issues to Session Log with severity and status
   - Add step status + commit hash to Session Log
```

**Step 2: Commit**

```bash
git add plugins/aid-orchestrator/skills/epic-orchestration.md
git commit -m "feat(orchestration): update session management integration for reviews and discovered issues"
```

---

### Task 7: Cross-reference verification

**Files:**
- Read all modified files and verify consistency

**Step 1: Verify**

Check that:
1. `decision-policies.yaml` `content_quality` section matches the rules in `epic-orchestration.md` PHASE_CHECK step 5
2. `decision-policies.yaml` `discovered_issues` section matches the triage logic in `epic-orchestration.md` PHASE_CHECK step 6
3. `run-epic.md` PHASE_CHECK references `decision-policies.yaml` sections correctly
4. `run-epic.md` agent prompt template mentions `## DISCOVERED ISSUES` format
5. `agent-core.md` DISCOVERED ISSUES format matches what playbooks describe
6. All 9 playbooks have identical DISCOVERED ISSUES block structure
7. Severity names are consistent: CRITICAL/HIGH/MEDIUM/INFO across all files

**Step 2: Fix any inconsistencies found**

**Step 3: Commit (if fixes needed)**

```bash
git commit -m "fix: cross-reference consistency for quality loop and discovered issues"
```
