# CP1-deep L3 (enforcement) — P080 (iteration 2)

_generated_by: aid-orchestrator:verifier@cp1-lens-L3
_generated_at: 2026-08-11T09:40:00Z
Reviewed-Head: cd1ab4af145b5272b200741ed41818ae058b2a18

stop_rule_blockers: []

resolution_verification:
  - id: L3-B1
    topic: "Step 4 cite resolution base (merged into AB-9)"
    status: RESOLVED
    check: |
      Plan lines 174-180 now specify the three real cite bases in resolution order
      (plugin root / repo root / plugin root + `scripts/`), name the mixed-base measurement,
      and add the rule "a token resolving under none of the three is a violation".
      Re-measured independently at this HEAD with a script implementing the plan's rule
      (skip status dead|removed_scoped, skip exact `n/a`/`planned`, split on `;` and ` + `,
      strip `:N`/`:~N`/`:<ident>`/possessive/annotations, try the three roots):
        rows with genuinely unresolved cites -> 9 (a crude subset of the adjudicator's 13):
        provenance_aggregate_fabricated, research_quality_gates, research_idempotency,
        knowledge_dedup_threshold, knowledge_base_write_protect, release_version_sealed,
        release_policy_preempted, test_audit_catalog_approval_boundary, test_audit_never_auto_invoked.
      The iteration-1 failure mode (764 flagged tokens on day one) is gone: the rule now
      produces a repairable single-digit/low-teens set, so the test can exit 0 after Step 4's
      repairs without being weakened.
      Also verified the `n/a` skip is exact-matchable: `python3` over the registry shows 75
      `n/a` values and ZERO `n/a …` variants, so "skip values `n/a`" is sufficient as written.
      Effort re-sized M -> L (plan line 199) per AB-9.

  - id: L3-B2
    topic: "Step 4 parser grammar (merged into AB-9)"
    status: RESOLVED
    check: |
      Plan line 178 enumerates the tokenising rule verbatim and cites all four annotation
      grammars present in the shipped file. My reimplementation of exactly that rule over
      `plugins/aid-orchestrator/defaults/enforcement-registry.yaml` (183 rows) produced no
      false-dangle explosion. AC1 (plan line 194) is now "flags exactly the rows enumerated
      in this step's Objective (re-measured at implementation time and reconciled in the
      verify output), PLUS a deliberately corrupted cite injected into a fixture as a
      negative control" — the negative control I asked for is present, and the arithmetically
      impossible "names exactly the aid-research rows" AC is gone.

  - id: L3-B3
    topic: "Step 4 repair list re-grounded (merged into AB-9)"
    status: RESOLVED
    check: |
      Plan line 166 now enumerates the measured 13 rows by id (11 active + 2 planned) and
      says re-measure + reconcile the delta in the verify output; line 180 adds the rule
      "a cite whose only resolution is an untracked path is itself a violation — repair the
      row, never allowlist the path". The adjudicator's correction to my CI framing is
      accepted and is not re-asserted here (`git ls-files .aid-o | wc -l` -> 165 at this HEAD;
      `.aid-o/plans/P069-…md` IS tracked, so it is not a CI-only failure).

  - id: L3-B4
    topic: "Gitignored in-repo fallback for the artifact spec page"
    status: RESOLVED
    check: |
      $ git check-ignore -v plugins/aid-orchestrator/defaults/templates/artifact-templates-spec.md
        -> (no output) exit=1  => NOT ignored
      $ git check-ignore -v plugins/aid-orchestrator/defaults/templates/   -> exit=1 (dir not ignored)
      Plan line 582 places the fallback there and states WHY the old path was wrong
      (`.gitignore:87` bare `docs/` matches at any depth). Step 16 AC (line 607):
      "Fallback path (if used): `git ls-files <chosen path>` returns the file" — tracked-file
      assertion, not mere existence. Class closed.

  - id: L3-B5
    topic: "Cross-repo Docusaurus deliverable — sidebar registration + commit authority"
    status: PARTIAL
    check: |
      $ git -C /opt/eco/docs rev-parse --show-toplevel -> /opt/eco/docs (separate repo, unchanged)
      $ grep -c autogenerated /opt/eco/docs/sidebars.ts -> 0 (still fully explicit)
      $ ls -d /opt/eco/docs/docs/aid/specs -> No such file or directory
      $ grep -n "aid/" /opt/eco/docs/sidebars.ts -> :519 'aid/intro', :525-527, :534… (explicit ids)
      RESOLVED half: plan line 581 adds `/opt/eco/docs/sidebars.ts` as an explicit modified file
      and states `_category_.json` is inert here; line 584 declares commit authority (separate
      PM-owned commit in the docs repo, exact command + resulting SHA recorded in verify output);
      Step 16 AC (line 606) is now `git -C /opt/eco/docs ls-files docs/aid/specs/artifact-templates.md`
      returns the file AND sidebars.ts references its id — falsifiable, existence alone rejected.
      PARTIAL half: the same AC ends with "If the PM commit is still pending, the step reports
      PM-pending with the exact command handed over", which makes the primary-path assertion
      self-satisfying at run time; and the plan-level machine AC block (lines 653-710, AC1-AC8)
      contains NO AC for the artifact spec page at all, so Success Criterion 6 ("the AID artifact
      spec page published") has no plan-level cmd verification. The in-repo fallback AC is the
      only unconditional mechanical assertion in the chain — acceptable, but it should be the
      declared floor, not an alternative that a PM-pending report can bypass.

  - id: L3-F1 (advisory -> `artifact_publication_wiring`)
    topic: "PM's binding Artifact requirement gets a mechanical check"
    status: PARTIAL
    check: |
      Plan line 345 (a2) adds the publication-wiring assertion to `test-communication-wiring.sh`;
      line 579 registers `artifact_publication_wiring` in the registry; plan-level AC5 runs that
      harness with expected_exit 0 — so unlike iteration 1 there IS a check that runs and blocks.
      What is still prose: the assertion has no defined match token. "each site that invokes a
      renderer ALSO carries the publish-before-present clause" names neither the literal to grep
      (Step 11 line 407 says "publishing the artifact body via the Artifact tool first";
      Step 12 line 441 says "artifact published before the card" — two different wordings) nor
      the enumerated set of renderer-invoking sites. An implementer can satisfy it with a grep
      for `Artifact` that any of the three files trivially matches.
      Fix (cheap): pin ONE literal clause in communication.md (e.g. `publish the artifact body
      via the Artifact tool BEFORE presenting the card`) and have Steps 11/12/14 paste it
      verbatim; the test asserts that literal at each of the enumerated wiring sites
      (aid-run.md GATES, pipeline.md gates phase, aid-plan.md plan-close, pipeline.md plan
      boundary). Also: the registry row's severity must be honest — this guard proves the
      INSTRUCTION TEXT is present, not that publication happened at runtime (publication is
      LLM-executed and unenforceable mechanically); the row's `instruction`/`surface` fields
      must say "wiring-presence guard", not "artifact published".

  - id: L3-F2 (advisory -> grandfathering decision)
    topic: "Removing the three rewritten files from test-skill-lint.sh's GRANDFATHERED list"
    status: PARTIAL
    check: |
      The plan now carries a "Grandfathering decision (CP1)" paragraph — but at ONE place only
      (line 159, inside Step 3) and as prose. `grep -n 'skill-lint\|aid-lint-skill\|GRANDFATHER'`
      over the plan returns lines 106, 108, 159, 364: no Files entry anywhere for
      `plugins/aid-orchestrator/scripts/tests/test-skill-lint.sh`, and no AC in Step 3 (154-158),
      Step 5 (230-235) or Step 6 (264-267) asserting the file was delisted or lints clean. The
      only lint AC in the plan is line 364, for the NEW skills/communication.md.
      Consequence: the mutation the decision requires (editing test-skill-lint.sh) is outside
      every step's declared outputs, so the scope gate refuses it, and nothing detects the
      "silent retention" the paragraph forbids.
      Verified against what the test actually does: `test-skill-lint.sh:32-51` GRANDFATHERED
      includes commands/aid-help.md, aid-init.md, aid-setup.md; lines 75-82 downgrade
      structural findings to `advisory` for those, universal findings still fail. So delisting
      is the whole mechanism — there is no other switch.
      Cost check (makes this nearly free): at this HEAD
        $ bash scripts/aid-lint-skill.sh commands/aid-help.md   -> no output (CLEAN)
        $ bash scripts/aid-lint-skill.sh commands/aid-setup.md  -> no output (CLEAN)
        $ bash scripts/aid-lint-skill.sh commands/aid-init.md   -> 3 structural
             version_stamped_heading at :134, :517, :533
      So Steps 3 and 6 can delist for free today; only Step 5 (aid-init.md) needs three heading
      renames or an explicit PM decision. Add `scripts/tests/test-skill-lint.sh` to the Files
      list of Steps 3/5/6 and one AC per step (`bash scripts/aid-lint-skill.sh <file>` empty AND
      the filename absent from the GRANDFATHERED heredoc).

  - id: L3-F3 (advisory -> Step 15 run-all-tests scoping)
    topic: "The invented `run-all-tests.sh` scoping flag"
    status: RESOLVED
    check: |
      $ sed -n '108,140p' plugins/aid-orchestrator/scripts/tests/run-all-tests.sh
        accepts only --verbose|-v, --list, --help|-h; `*)` -> "Unknown argument" exit 1.
      Plan line 549 now says explicitly it is "**Not** a 'scoped run-all-tests.sh invocation'",
      cites the :132 unknown-argument exit, and offers the two real forms (explicit per-suite
      `bats`/`bash`, or one full `--verbose` run) with "state which, and record the command in
      the verify output"; AC2 (line 567) is worded against the declared form plus a log excerpt.
      No invented flag remains.

  - id: L3-F5 (advisory -> unregistered checks)
    topic: "Every new mechanical check gets a registry row"
    status: RESOLVED
    check: |
      Plan line 579 now lists nine rows: help_index_coverage, registry_cite_validation,
      init_workspace_idempotency, communication_reference_wiring, handoff_renderer_contract,
      artifact_template_caps, artifact_publication_wiring, config_summary_read_only,
      step_seam_human_rendering — the two I flagged as missing (Step 7 read-only contract,
      Step 13 step-seam regression) are both present.
      Confirmed there is no pre-existing row to fold into for the step seam:
      $ grep -n 'fsm-step-render\|Step rendering rule\|executing_step' \
          plugins/aid-orchestrator/defaults/enforcement-registry.yaml -> NO MATCH
      (the shipped P073 bats suite has never been registered — so `step_seam_human_rendering`
      is a genuinely new registration, not a duplicate).

  - id: L3-F6
    topic: "8-location version registry has no mechanical check"
    status: UNRESOLVED (accepted by omission)
    check: |
      Unchanged at this HEAD: .github/workflows/version-sync.yml still checks only the two
      package.json files with the plugin check commented out; no
      `plugins/aid-orchestrator/defaults/policies/release-policy.yaml` exists (the only
      release-policy.yaml on disk is the gitignored `.aid-o/config/` copy).
      Plan Step 16 AC1 (line 604) is still "All 8 version locations agree (the CLAUDE.md
      pre-push grep set)" — a human grep, not a runnable cmd, and it is not among the
      plan-level AC1-AC8 verification_patterns. The adjudicator asked for an explicit
      decision ("decide explicitly whether version sync stays PM-manual"); the revision
      neither adds the 20-line harness nor records the decision. Not a blocker (the class
      predates this plan and the release_version_sealed row exists), but Principle #1 says
      an unenforced 8-location invariant should at least be labelled as PM-manual in the step.

  - id: L3-C4 (new check requested by the dispatcher)
    topic: "Row-id uniqueness assertion"
    status: RESOLVED
    check: |
      Plan line 169 folds `yq '.enforcements[].id' | sort | uniq -d` must be empty into Step 4's
      test; Step 4 AC3 (line 196) and Step 16 AC (line 609) both assert it, and Step 16 line 579
      cites it as the reason an append re-run cannot silently duplicate a row.
      Verified the expression is valid against the shipped file:
      $ yq '.enforcements[].id' plugins/aid-orchestrator/defaults/enforcement-registry.yaml | head -3
        plan_final_gate_required / plan_final_specialist_review / epic_specialist_review_exception
      (top-level key IS `enforcements`, 183 rows) — the assertion runs as written.
      Note it only covers the DUPLICATE-row half of the non-idempotency class; the double-bump
      half is handled by prose at line 592 ("idempotent by declaration") with no assertion.

findings:
  - id: L3-F9
    severity: high
    step: 3, 5, 6
    finding: "The grandfathering decision is prose in one step with no Files entry and no AC — see L3-F2 above. As written it cannot be executed (scope gate) and cannot be detected if skipped."
    evidence: |
      grep over the plan: `test-skill-lint.sh` appears in NO Files list; the decision text
      lives only at plan line 159 inside Step 3 while binding Steps 5-6.
      test-skill-lint.sh:32-51 GRANDFATHERED + :75-82 advisory downgrade is the only mechanism.
      Delisting is free for aid-help.md and aid-setup.md (both lint clean today) and costs three
      `version_stamped_heading` fixes for aid-init.md (:134, :517, :533).
    recommendation: "Add `plugins/aid-orchestrator/scripts/tests/test-skill-lint.sh` to the Files list of Steps 3, 5 and 6, and one AC per step: `bash plugins/aid-orchestrator/scripts/aid-lint-skill.sh <file>` prints nothing AND `grep -qxF '<file>' ` against the GRANDFATHERED heredoc returns 1."

  - id: L3-F10
    severity: high
    step: plan-level AC7
    finding: "AB-3 was applied to Step 14 but NOT to the plan-level AC — AC7 still greps a single file, so the second hardcoded-Czech file it exists to catch cannot fail it."
    evidence: |
      Plan line 513 (Step 14 Files) and line 537 (Step 14 AC3) both correctly cover
      `commands/aid-verify-plan.md`, but plan line 701 still reads:
        cmd: "bash -c '! grep -n \"in \\*\\*Czech\\*\\*\" plugins/aid-orchestrator/commands/aid-verify-implementation.md'"
      The adjudicator's required_revision for AB-3 named the widened form explicitly:
        bash -c '! grep -rn "in \*\*Czech\*\*" plugins/aid-orchestrator/commands/'
      Plan-level ACs are what the DONE/C3 evidence check evaluates, so the narrow one is the
      one that ships.
    recommendation: "Replace AC7's cmd with the recursive form over `plugins/aid-orchestrator/commands/` exactly as AB-3 specified."

  - id: L3-F11
    severity: medium
    step: 9, 11, 12, 14, 16
    finding: "`artifact_publication_wiring` runs and blocks, but its match token and its site list are undefined, and the registry row's severity/scope must not overstate what it proves (instruction presence, not runtime publication)."
    evidence: "See resolution_verification L3-F1 — two different wordings already exist across Steps 11 (line 407) and 12 (line 441); no literal is nominated as canonical."
    recommendation: "Nominate one canonical clause in communication.md, paste it verbatim at the four enumerated wiring sites, assert that literal in test-communication-wiring.sh, and word the registry row as a wiring-presence guard."

  - id: L3-F12
    severity: medium
    step: 2, 15
    finding: "CI budget headroom is still unaccounted for — the revision did not touch it (L3-F4 was not carried into the adjudicator's accepted or advisory sets)."
    evidence: |
      Measured at this HEAD: 41 flat `test-*.sh` + 150 `bats/test-*.bats` = 191 discovered,
      minus 5 DELEGATED_SUITES (run-all-tests.sh:150-165) = 186 inline suites in the
      `bash-tests` job. ci.yml's own comment on that step records a real run that "only reached
      suite 53/131 in its 20-minute budget"; the current timeout is 60 min.
      This plan adds NINE inline suites (correcting my iteration-1 split): 4 flat —
      test-enforcement-registry-cites.sh, test-init-idempotency.sh, test-communication-wiring.sh,
      test-integration-handoff-rendering.sh — and 5 bats — test-help-index-coverage.bats,
      test-aid-config-summary.bats, test-aid-artifact-render.bats,
      test-aid-gate-outcome-summary.bats, test-aid-plan-close-summary.bats.
      test-init-idempotency.sh is the heavy one (mktemp repo + git init + two full scripted
      replays + sha256sum tree diffs).
      Positive re-verification: auto-discovery still works for both shapes
      (run-all-tests.sh:172 `test-*.sh`, :177 `bats/test-*.bats`) and ci.yml runs it on push
      AND pull_request, so Success Criterion 1 ("fails CI until intentionally indexed") holds.
    recommendation: "Add one sentence to Step 15: measure the bash-tests wall clock after the additions and, if the job approaches its 60-min budget, delegate test-init-idempotency.sh (or the heaviest measured suite) to its own CI job via the DELEGATED_SUITES mechanism."

  - id: L3-F13
    severity: medium
    step: 13
    finding: "The test-fsm-step-render.bats rewrite does not weaken drift protection overall, but the replacement assertion has no nominated literal, and one guard the current test carries per-file disappears without a stated substitute."
    evidence: |
      Current shipped block (`:103-116`) loops the five surfaces asserting BOTH
      `grep -c 'Step rendering rule' >= 1` AND
      `grep -c 'executing_step = min(current_step + 1, total_steps)' >= 1` per file.
      After the collapse only ONE file carries the formula, so the second per-file assertion
      must go. Plan line 478 replaces it with "each of the five surfaces contains a REFERENCE
      to the one authoritative section" + "exactly one file states the full rule" — neither
      the reference literal nor the "full rule" detector literal is named.
      What still holds after the rewrite (verified as still present in the same @test):
        - the two negative greps `{current_step}/{total_steps}` and `{current_step} of {total_steps}`
          across commands/ + skills/, and the uncapped-+1 guard;
        - the four verbatim `(human: step N of T is next|complete)` assertions at :33/:41/:49/:76,
          which the plan updates rather than deletes;
      so the five surfaces cannot silently revert to rendering the raw 0-based value.
      Also note the current six copies are byte-identical across five files
      (pipeline.md:1322 + :2756, aid-run.md:402, aid-status.md:984, aid-stop.md:119, memory.md:28),
      and _fsm_human_step really has three call sites (:2858, :2870, :5246) — both corrections
      landed in the plan (lines 469-478).
    recommendation: "Name the two literals in Step 13: the reference token (e.g. `see skills/pipeline.md § Step rendering rule`) asserted once per surface, and `executing_step = min(current_step + 1, total_steps)` whose repo-wide count must equal 1 — that pair makes AC line 497 ('one full definition plus five references') mechanically checkable instead of eyeballed."

  - id: L3-F14
    severity: low
    step: 16
    finding: "CLAUDE.md's mandatory-registration instruction still points at a path that does not exist, and the plan cannot fix it in-repo."
    evidence: |
      $ grep -n 'enforcement-registry' CLAUDE.md
        :195 "recorded in the enforcement registry (`docs/plans/AID-audit-2026-06/enforcement-registry.yaml`)"
      $ git check-ignore -v CLAUDE.md -> .gitignore:107:CLAUDE.md  (gitignored — a step cannot ship a fix)
      The real registry is `plugins/aid-orchestrator/defaults/enforcement-registry.yaml`
      (309 KB, 183 rows), which the plan correctly targets. `docs/extending-aid.md` IS tracked
      and is already in Step 16's Files list.
    recommendation: "State the correct registry path in the `docs/extending-aid.md` section Step 16 already writes, and hand the CLAUDE.md line to the PM as a one-line note in the step's verify output (the file is gitignored, so it is out of any step's reach)."

  - id: L3-F15
    severity: low
    step: 16
    finding: "Positive verification — the fallback path is not merely un-ignored, the directory itself is tracked and populated, so a new file there is reviewable by default."
    evidence: |
      $ git check-ignore -v plugins/aid-orchestrator/defaults/templates/ -> exit 1 (not ignored)
      $ ls plugins/aid-orchestrator/defaults/templates/ -> 17 tracked templates
        (plan.md, epic.md, verifier-output-template.md, …)
    recommendation: "No change."

confidence: high
