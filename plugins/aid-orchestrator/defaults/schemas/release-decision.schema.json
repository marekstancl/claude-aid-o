{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aid-orchestrator.aidlab.dev/schemas/release-decision.schema.json",
  "title": "AID Protocol v2 — ReleaseDecision",
  "$comment": "producer: C4, consumers: [FSM, PM brief, GUI]",
  "description": "Artifact emitted by C4 (release controller) containing the final release readiness decision and references to all evidence inputs that contributed to it.",
  "type": "object",
  "allOf": [
    {
      "$comment": "extends aid-protocol-v2.schema.json envelope (reference only — not a resolved $ref)"
    }
  ],
  "properties": {
    "artifact_type": {
      "const": "release_decision",
      "$comment": "enforced — must equal this type"
    },
    "release_decision": {
      "type": "object",
      "$comment": "enforced — minimal payload; full schema defined in owning phase",
      "properties": {
        "release_ready": {
          "type": "boolean"
        },
        "inputs": {
          "type": "array"
        }
      }
    }
  },
  "required": ["release_decision"]
}
