{
  "epic_id": "E-TEST-001-1_1",
  "source_plan": "plugins/aid-orchestrator/scripts/tests/fixtures/minimal-plan.md",
  "version": 1,
  "created_at": "2026-02-28T00:00:00Z",
  "steps": [
    {
      "id": "step_1_backend",
      "role": "backend",
      "objective": "Implement the core module with basic data structures and helper utilities.",
      "inputs": ["EPIC specification"],
      "outputs": ["src/core/module.py", "src/core/utils.py"],
      "constraints": ["Python 3.10+ only"],
      "allowed_paths": ["src/core/module.py", "src/core/utils.py"],
      "forbidden_paths": [],
      "acceptance_criteria": [
        "Module loads without errors",
        "Helper functions return expected types"
      ]
    },
    {
      "id": "step_2_qa",
      "role": "qa",
      "objective": "Create a comprehensive unit test suite covering all public functions.",
      "inputs": ["EPIC specification", "Output from step_1_backend"],
      "outputs": ["tests/test_module.py"],
      "constraints": ["Python 3.10+ only"],
      "allowed_paths": ["tests/test_module.py"],
      "forbidden_paths": ["src/core/"],
      "acceptance_criteria": [
        "All public functions have at least one test",
        "Test coverage exceeds 80 percent"
      ]
    }
  ],
  "dependencies": [
    {
      "before": "step_1_backend",
      "after": "step_2_qa",
      "reason": "QA tests require the backend implementation to exist"
    }
  ],
  "parallel_groups": [],
  "analysis_groups": [],
  "gates": ["tests_pass", "docs_updated"],
  "budget": {
    "max_retries_per_gate": 3
  }
}
