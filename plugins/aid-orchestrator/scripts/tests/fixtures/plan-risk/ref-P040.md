---
id: REF-P040
type: plan
status: draft
---

<!-- Reference fixture for the plan ceremony-band classifier (P084 Step 1).
     Source: .aid-o/plans/P040-dispatch-lifecycle.md (a real plan of this repo; .aid-o/ is gitignored, so the
     Files declarations are reproduced here to keep the reference runnable).
     The frontmatter risk: field is deliberately NOT reproduced — this fixture
     exercises the path map, and the frontmatter raise has its own case. -->

# Reference fixture P040

## Implementation Steps

### Step 1: the source plan's declared files

**Files:**
- Create: `plugins/aid-orchestrator/scripts/aid-emit-dispatch.sh` — new bash CLI; sources `lib/aid-stage-log.sh` for `log_event` helper; implements `start` and `complete` subcommands per Component A architecture
- Create: `plugins/aid-orchestrator/scripts/tests/bats/test-aid-emit-dispatch.bats` — bats fixtures for the five scenarios listed below
- Modify: `plugins/aid-orchestrator/scripts/aid-fsm.sh` (insert new `fsm_check_orphan_dispatches()` between line 143 `fsm_check_verifier_output` and line 172 `fsm_handle_force_override`; wire call into `cmd_increment_step` at line 1369-1555 after existing memory_used/memory_written/verifier_output checks and before state transition write)
- Modify: `plugins/aid-orchestrator/defaults/check-severity.yaml` (append `dispatch_orphan_complete` entry with severity: blocking, per Component B architecture spec)
- Test: `plugins/aid-orchestrator/scripts/tests/bats/test-aid-fsm.bats` (extend existing file with 3 new fixtures: clean pending → pass, orphan start → block, --force --blocked-checks dispatch_orphan_complete → bypass with audit log)
- Modify: `plugins/aid-orchestrator/scripts/aid-fsm.sh` (insert new `fsm_check_cp4_curator_validation()` between `fsm_check_orphan_dispatches` from Step 2 and `fsm_handle_force_override`; wire call into `cmd_done_advance` review→release at line 1555+ BEFORE existing curator-report / audit-report / pm_decision / P1 security checks)
- Modify: `plugins/aid-orchestrator/defaults/check-severity.yaml` (append `cp4_curator_validation` entry with severity: blocking)
- Modify: `plugins/aid-orchestrator/defaults/execution.yaml` (append new top-level field `cp4_production_paths: "plugins/|scripts/|src/|lib/|api/"` — AID-orchestrator-self default; consumer projects override via `/aid-init` auto-detection per next bullet)
- Modify: `plugins/aid-orchestrator/scripts/lib/aid-init-execution-yaml.sh` — extend stack-scan composer to write a project-specific `cp4_production_paths` value when stack signatures are detected: Node/TS monorepo (`apps/|services/|packages/|src/`), Payload/Next CMS (`src/apps/|src/payload\.config\.ts|src/collections/`), Python service (`src/|app/|api/|services/`), AID plugin self (`plugins/|scripts/|src/|lib/|api/`). When zero stacks match, write the AID-self default plus a `# WARNING: stack auto-detect produced no match — verify cp4_production_paths reflects your project's production-code layout` comment. Verified via fixture in Step 3 bats: consumer-project mock with `apps/` layout → cp4_production_paths written as `apps/|services/|packages/|src/`
- Test: `plugins/aid-orchestrator/scripts/tests/bats/test-aid-fsm.bats` (extend with 5 fixtures: (1) no curator-report → skip, (2) curator commit at HEAD touches production + missing CP4 → block, (3) curator commit at HEAD~1 touches production AND PM docs edit at HEAD → block via range scan (NR 10 §3B regression fixture), (4) entire base_commit..HEAD range is docs-only → skip with `cp4_skip_no_prod_match` telemetry event, (5) consumer-project layout fixture: cp4_production_paths set to `apps/|services/|packages/` and curator commit touches `apps/foo.ts` → block)
- Modify: `plugins/aid-orchestrator/scripts/aid-fsm.sh` — extend `cmd_init` (line 934-1170) arg parser to accept `--streamlined` flag and write `streamlined_mode: true` field into fsm-state.yaml; extend `cmd_increment_step` (line 1369-1555) to read `streamlined_mode` and skip per-step CP2 verifier_output check when true; insert two new functions — `fsm_check_streamlined_integration_review()` (enforces CP3 code-review + security + gates_report files present) and `fsm_check_streamlined_abandoned()` (timeline event-count check) — between `fsm_check_cp4_curator_validation` from Step 3 and `fsm_handle_force_override`; wire BOTH new functions into `cmd_done_advance` review→release BEFORE Component C's CP4 check (integration_review first, then abandoned); make `fsm_check_cp4_curator_validation` mode-aware (early-return with `cp4_skipped_streamlined_advisory` audit event when streamlined_mode=true); extend `write_compliance_json` (line 635-771) to emit `coverage_mode` and `skipped_dimensions` top-level fields (skipped_dimensions when streamlined contains BOTH `verifier_outputs.cp2_per_step` AND `verifier_outputs.cp4_curator_validation`)
- Modify: `plugins/aid-orchestrator/defaults/check-severity.yaml` (append TWO entries with severity: blocking — `streamlined_abandoned` AND `streamlined_integration_review`; Component B and Component C add `dispatch_orphan_complete` and `cp4_curator_validation` entries respectively per their own Files lists, so the registry gains FOUR new blocking entries total across Steps 2-4)
- Modify: `plugins/aid-orchestrator/commands/aid-run.md` — document `--streamlined` flag (forwarded from /aid-run CLI to `cmd_init`); cross-reference fsm-state.yaml schema
- Modify: `plugins/aid-orchestrator/commands/aid-plan.md` — advisory trigger criteria section: when 0 logic-changing files + <5 files modified + <100 LOC delta + 0 security_sensitive paths → /aid-plan suggests `--streamlined`
- Test: `plugins/aid-orchestrator/scripts/tests/bats/test-aid-fsm.bats` (extend with 7 fixtures per Part 7 below: streamlined init writes streamlined_mode field, streamlined skips per-step CP2 in cmd_increment_step, streamlined compliance.json has coverage_mode + 2-entry skipped_dimensions, abandoned detection fires on 2-event timeline (NR 12 P009 anchor), integration-review missing CP3 file blocks, integration-review all-three-present passes, CP4 mode-aware skip in streamlined)
- Modify: `plugins/aid-orchestrator/scripts/aid-json-to-run.sh` (610 lines, Steps 1-17 structured) — append new Step 18 block after the existing Step 17 absolute-path echo (line ~603); idempotent FSM init call
- Modify: `plugins/aid-orchestrator/scripts/aid-fsm.sh` — extend `cmd_init` (line 934-1170) to absorb the legacy `steps[]` array field into fsm-state.yaml (currently writes via separate state.yaml convention in some projects); add backward-compat reader logic in any function reading the steps array
- Modify: `plugins/aid-orchestrator/skills/pipeline.md` (PRE-FLIGHT subsection, location resolved at Step 5 implementation via `grep -n PRE-FLIGHT plugins/aid-orchestrator/skills/pipeline.md`) — explicit statement that aid-json-to-run.sh auto-initializes FSM state
- Modify: `plugins/aid-orchestrator/skills/pipeline.md` — (a) new "Dispatch Protocol" subsection in §4 between existing CP2 and CP3 sections that documents the mandatory `aid-emit-dispatch.sh start` → `Agent({subagent_type, prompt})` → `aid-emit-dispatch.sh complete` chain (cross-references AID-v3-principles.md §1); (b) **REPLACE the six existing inline `aid-stage-log.sh log_event` snippets** at the verified line numbers (as of 2026-05-31): 401, 422 (CP2 start/complete), 467, 475 (CP3 dual start), 493, 502 (CP3 dual complete) — replace each with the equivalent `aid-emit-dispatch.sh start` / `complete` call so that timeline events are no longer double-emitted (orchestrator writes via wrapper; wrapper internally calls `log_event`); (c) add **new CP4 dispatch protocol subsection** in §7 (release sub-phase context) wrapping the `cp4-curator-validation` Agent() dispatch with start/complete wrapper calls — verified via `grep -n 'log_event' pipeline.md` (6 hits at lines 401/422/467/475/493/502) and `grep -n 'CP4' pipeline.md` for insertion anchor. Locations resolved at Step 6 impl time via fresh `grep -n`.
- Modify: `plugins/aid-orchestrator/skills/agent-protocol.md` — reference table listing all NINE new audit event types introduced by P040 — four blocking failures (`fsm_orphan_dispatch_fail`, `cp4_missing_fail`, `streamlined_abandoned_fail`, `streamlined_integration_review_fail`) + five advisory/telemetry events (`dispatch_completed_late`, `fsm_orphan_dispatch_waived`, `cp4_skip_no_prod_match`, `cp4_skipped_streamlined_advisory`, `cp4_glob_evaluated`) — plus four new check-severity entries (`dispatch_orphan_complete`, `cp4_curator_validation`, `streamlined_abandoned`, `streamlined_integration_review`)
- Modify: `plugins/aid-orchestrator/commands/aid-plan.md` — advisory trigger criteria section (per Component D scope from Step 4); criteria: 0 logic-changing files, <5 files modified, <100 LOC delta, 0 security_sensitive globs
- Modify: `plugins/aid-orchestrator/commands/aid-run.md` — document `--streamlined` CLI flag (per Component D scope from Step 4); cross-reference fsm-state.yaml streamlined_mode field
- Modify: `plugins/aid-orchestrator/commands/aid-do.md` — note that Fast Mode equivalent of --streamlined for sub-2-min tasks; future follow-up may auto-set
- Modify: `CHANGELOG.md` (root) — append `## [2.25.0] — 2026-05-31` section with Added (5 functions/script/severity entries/flag) + Changed (cmd_increment_step preconditions, cmd_done_advance preconditions, aid-json-to-run.sh auto-init, write_compliance_json schema) + cite AID-v3-principles.md §1 + NR 8-14 anchors
- Modify: `plugins/aid-orchestrator/CHANGELOG.md` — byte-identical copy of root CHANGELOG.md v2.25.0 section
- Modify: `CHANGELOG.md` (root) — `## [2.25.0] — 2026-05-31` header (already populated by Step 6 with full Added/Changed subsections)
- Modify: `plugins/aid-orchestrator/CHANGELOG.md` — `## [2.25.0] — 2026-05-31` header (byte-identical, populated by Step 6)
- Modify: `.claude-plugin/marketplace.json` — `metadata.version` JSON field (regex: `"version": "2.24.[0-9]+"` → `"version": "2.25.0"`)
- Modify: `.claude-plugin/marketplace.json` — `plugins[0].version` JSON field (same JSON file, separate field at different JSONPath)
- Modify: `plugins/aid-orchestrator/.claude-plugin/plugin.json` — `version` JSON field (top-level)
- Modify: `plugins/aid-orchestrator/README.md` — `- **Plugin:** 2.25.0` line (regex: `\*\*Plugin:\*\* 2\.24\.[0-9]+` → `**Plugin:** 2.25.0`)
- Modify: `README.md` — `- **v2.25.0** (current)` Roadmap line (existing current moves to second position, oldest drops off; Roadmap holds 3 most recent versions per CLAUDE.md "Keep the 3 most recent versions" rule)
- Modify: `README.md` — `AGPL-3.0-only — see [LICENSE](LICENSE)` exact-string check (no version embedded; verify the line is untouched per CLAUDE.md Registry #8 — this is a stability check, not a bump)
