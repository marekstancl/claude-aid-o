_generated_by: aid-orchestrator:verifier@s11-acta-E-020-2_3-step1
_generated_at: 2026-09-19T00:00:00Z
classification: FULL_REVIEW
verdict: pass
findings: []

## Checks performed (repo: /opt/eco/projects/acta @ 0a6d2fbced97696ee1bfb026e69222349454a3ee, read-only via git show/git grep)

- No list (`ClientDocuments.tsx`, `ClientArchive.tsx`, `Dashboard.tsx`) renders `ConfidenceDot`/`ConfidencePct` any more — all three now render a plain span keyed off `review_field_count`, with `—` for zero/missing (`reviewCountLabel` helper, duplicated per-file but consistent).
- `rowClassName` in `ClientDocuments.tsx` (frontend/src/components/client/ClientDocuments.tsx:323) and `Dashboard.tsx` (frontend/src/pages/Dashboard.tsx:311) now branch on `doc.review_field_count > 0`, no `min_confidence` left. `ClientArchive.tsx` correctly does not add a `rowClassName` (none existed pre-diff — confirmed via `git show e128354b:.../ClientArchive.tsx | grep rowClassName` → no match), matching the DoD note that "totéž jako ClientDocuments neplatí" here.
- `grep -rn --exclude-dir=__tests__ "calcConfidencePct" frontend/src` at the target commit returns nothing (verified with `git grep`); the only remaining occurrence is the negative-assertion test in `frontend/src/lib/__tests__/format.test.ts:60-64`.
- `components/shared/ConfidenceDot.tsx` and its only caller, `pages/DocumentDetail.tsx` (uses `level`/`ConfLevel` props, a distinct component from `documents/ConfidenceBadge.tsx`'s `ConfidenceDot`), are untouched by the diff.
- `components/documents/ConfidenceBadge.tsx` (a different `ConfidenceDot`/`ConfidencePct` pair, still used by `EditableField.tsx` and `pages/help/ZakladyPage.tsx`) keeps working: it now inlines its own `computeConfidencePct` instead of importing the deleted `calcConfidencePct` from `lib/format.ts`. Consistent with the test's comment that removing this pair is deferred to Step 7 — out of this step's scope, and not broken by it.
- `types/document.ts`: `min_confidence` removed from `Document`, replaced by `review_field_count: number` and `review_reasons: ReviewReason[]`; `FieldMeta.confidence` removed. `git grep min_confidence` across `frontend/src` at the target commit returns zero hits — no dangling references.
- `components/documents/ExportButton.tsx`: `filters.confidence` field and its query-param wiring removed; no other caller passes `confidence` into `ExportFilters` (checked call sites in `pages`/`components/client`).
- Type-safety spot check for the one place `FieldMeta.confidence` was still read outside the diff's scope, `EditableField.tsx:30` (`meta?.confidence`): `meta` comes from `doc.fields_meta?.[fieldKey]`, and `Document.fields_meta` is typed `Record<string, any> & {...}` — the lookup resolves to `any`, so removing `confidence` from the `FieldMeta` interface does not produce a `tsc` error here. (Could not actually execute `tsc --noEmit`, since the working tree is checked out at a later commit, b49b3aa, and the task requires read-only inspection at the named commit without checking anything out; verified statically instead — no type contradictions found in the changed or adjacent files.)
- Scope/forbidden-path check: diff touches only files listed in `step_outputs` (`format.ts`, `ClientDocuments.tsx`, `ClientArchive.tsx`, `Dashboard.tsx`, `document.ts`, `ExportButton.tsx`, `format.test.ts`, plus `ConfidenceBadge.tsx` which is a necessary consequence of deleting the shared `calcConfidencePct` export and stays behaviorally identical). No backend files, no `segmentation.py`, no DUNA mapping, no escalation/checker/validation code, no dashboard nav, no popover, no `min_confidence` DB/column work, no rename to `validation_level`, no persistent quality-metrics/dashboard code touched — all `step_forbidden_paths` respected.
