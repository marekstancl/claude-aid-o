_generated_by: aid-orchestrator:verifier@s04-wan-E-091-3_3-step1
_generated_at: 2026-09-19T00:00:00Z
classification: FULL_REVIEW
verdict: pass
findings: []

## Notes (non-blocking)

- `wan/pipeline/pipeline.py:887-890` (`_process_intake_file`): recycling is only attempted `if intake_file is not None`, i.e. it never applies on the legacy `run_pipeline` path without an evidence row. This is consistent with `najdi_dvojce` requiring `IntakeFile.sha256`, and does not violate any AC ("soubor bez dvojčete se zpracuje přesně jako dnes" only requires no-twin files to be unaffected), but the inline comment ("tudy projde každý zpracovávaný soubor") slightly overstates coverage — worth a one-line docstring correction, not a functional defect. Severity: low / doc-only.

## Verification performed (read-only, commit 4cad57beaab17a3a375d01bd137ee50d8829abf7)

- `slouc_do_dat_davky` (wan/pipeline/pipeline.py:899-978) extracted from `_apply_result` verified byte-for-byte equivalent to the pre-change body (merge → diacritic restoration + warning sink → gdpr empty-OM sanitize → setattr), confirmed against `_apply_result`'s original source at the same commit. `_apply_result` calls it unchanged (AC: "vyčleněná čistá funkce, beze změny chování").
- `wan/db/enums.py:100-107`: `"recycle"` added to `PipelineStep` Literal, picked up by `PIPELINE_STEPS` via `get_args` — matches step_outputs.
- `_zkus_recyklaci` (pipeline.py:827-865) sits in `_process_intake_file` before the `_run_steps` call (pipeline.py:887-890), catches bare `Exception`, rolls back and refreshes both ORM objects on failure, returns `None` to fall through to the normal read path — satisfies GEN-002 (recycling failure must not break the main path) and the ordering requirement (verified independently by `tests/meta/test_intake_recycle_wiring.py::test_recyklace_sedi_pred_ctenim` and `test_recyklace_je_fail_safe`).
- `wan/pipeline/recycle.py` (new): `postav_kopii` copies only `KOPIROVANA_POLE` (explicit whitelist, no `__dict__`/`copy`/`vars`/`__table__` loop present) and takes `session_id`, `person_id`, `source_intake_file_id`, `minio_key` (computed via `doc_page_key`, not copied), `ocr_model_used="recycled"`, `ocr_duration_ms=0`, `ocr_total_cost_usd=0.0`, `ocr_tier=None`, `tier_reason=None` from the target, not the twin — matches ACs on `source_intake_file_id`/`person_id`/`minio_key` prefix, absence of `delivery_point_id`/`supply_contract_id`/`contract_group_id`, and `ocr_model_used="recycled"` with zero cost / no shadow run.
- `prevezmi_z_dvojcete` iterates `twin.aktivni_doklady` sorted by `ordinal`, copies page previews via `_zkopiruj_stranky` (raises if zero pages copied — no unreadable-doc silently created), calls `slouc_do_dat_davky` per non-failed doc into a throwaway `SimpleNamespace` warning sink (avoids double "OCR opraveno" — matches AC), forwards the twin's `IntakeFile.validation_warnings` via `_append_warnings(intake_file, ...)`, writes a single `trace_step(..., "recycle", "success", ...)`, and commits exactly once — matches "krok recycle in pipeline_traces" and "warnings transferred, not duplicated".
- Signatures used from existing code (`trace_step`, `FileResult`, `_append_warnings`, `doc_page_key`, `minio.list_keys/download/upload`) all match their definitions at this commit; the deferred `from wan.pipeline.recycle import prevezmi_z_dvojcete` inside `_zkus_recyklaci` avoids the module-level circular import correctly (recycle.py imports from pipeline.py at module level; pipeline.py only imports recycle.py inside the function body).
- No touches to any `step_forbidden_paths` area (V2/V4/annotation-of-changed-fields/production-duplicate-migration/`overlap_exists_sql`/V8-V10/Z1-Z6/MAX_OM/`manual_group_service.py`) — diff is confined to `wan/pipeline/pipeline.py`, `wan/pipeline/recycle.py`, `wan/db/enums.py`, and the three named/added test files.
- Test coverage (`tests/integration/test_intake_recycle_pipeline.py`, `tests/unit/pipeline/test_recycle_kopie.py`, additions to `tests/meta/test_intake_recycle_wiring.py`) exercises every AC bullet: no model call on twin match, `session.{type}_data` parity, correct target-batch linkage, absence of foreign-client fields, `ocr_model_used="recycled"`/zero cost/no shadow run, warning transfer without duplication, `recycle` step present, and the untouched no-twin path — plus a fail-safe test that a broken `prevezmi_z_dvojcete` doesn't corrupt the batch or leave duplicate documents. Tests were read, not executed (no DB/MinIO available to this verifier), so their assertions were checked for correctness against the diff logic rather than confirmed by a live run.
