# C3 Dogfood (real AC) — Live Attestation [fail] (P065 IMP-245 follow-up)

Committed PROVENANCE PROOF that the C3 bridge, given a REAL (tiny) acceptance
criteria document via `AID_PLAN_AC_FILE`, reached the determinate verdict
`fail` from a real live Codex CLI dispatch — not `unverifiable`.
Raw evidence is not committed; `c3-dogfood-fixture-real-ac-fail/` is a separate, sanitized,
re-hashed regression fixture (proves `verify`'s logic, not external provenance).

| Field | Value |
|---|---|
| codex_version | `` |
| codex_session_id (prefix only) | `019f7ea1...` |
| achieved_independence_level | `cross_provider` |
| expected status | `fail` |
| observed status | `fail` |
| run_at (UTC) | `2026-07-20T08:27:09Z` |
| live_verify | passed |

live_verify: passed
