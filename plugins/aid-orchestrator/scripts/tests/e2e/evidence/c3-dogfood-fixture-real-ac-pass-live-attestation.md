# C3 Dogfood (real AC) — Live Attestation [pass] (P065 IMP-245 follow-up)

Committed PROVENANCE PROOF that the C3 bridge, given a REAL (tiny) acceptance
criteria document via `AID_PLAN_AC_FILE`, reached the determinate verdict
`pass` from a real live Codex CLI dispatch — not `unverifiable`.
Raw evidence is not committed; `c3-dogfood-fixture-real-ac-pass/` is a separate, sanitized,
re-hashed regression fixture (proves `verify`'s logic, not external provenance).

| Field | Value |
|---|---|
| codex_version | `` |
| codex_session_id (prefix only) | `019f6ad3...` |
| achieved_independence_level | `cross_provider` |
| expected status | `pass` |
| observed status | `pass` |
| run_at (UTC) | `2026-07-16T12:10:04Z` |
| live_verify | passed |

live_verify: passed
