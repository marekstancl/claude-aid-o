<!--
  Fixture for parseScope AC4 (EPIC E-047-2_7, Step 2).
  A Scope section with NO recognized "### Allowed files/paths" /
  "### Forbidden zones" sub-headings — only prose and an unrelated bullet list.
  parseScope must return empty arrays + the raw body, never throw.
-->

## Scope

This EPIC has no structured scope sub-headings. It is described in prose:
the change is confined to the parser package and touches nothing else.

- some unrelated bullet that is not under an allowed/forbidden heading
- another prose bullet
