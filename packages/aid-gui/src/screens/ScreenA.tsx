import { ScreenStub } from './ScreenStub';

/** Přehled — cross-project infra overview. Filled in Phase 6. */
export function ScreenA() {
  return <ScreenStub title="Přehled" todoStep="Step 37" />;
}
